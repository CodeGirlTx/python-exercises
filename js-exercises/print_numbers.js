function print_number (num1, num2) {
  for (var i = num1; i <= num2; i++) {
    console.log(i);
  }
}

function print_number2 (num1, num2) {
  count = num1 - 1;
  while (count < num2) {
    count += 1;
    console.log(count);
  }
}
