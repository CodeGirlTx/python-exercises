from turtle import *
degrees()
def full_star(size):

    for side in range(5):
        fillcolor(color)
        pencolor("white")
        begin_fill()
        forward(size)
        left(36)
        forward(size)
        right(108)
        end_fill()

draw_star(75)

mainloop()
